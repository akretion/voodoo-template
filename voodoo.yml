odoo:
  build: /home/sebastien/DEV/PAAS/voodoo-image
  environment:
    - POSTGRESQL_DATA=/workspace/.db
    - POSTGRESQL_DB=db
    - VIRTUAL_HOST:test.localhost
  volumes:
    - .:/workspace
  ports:
    - "$odoo_port:8069"
    - "$odoo_polling_port:8072"
